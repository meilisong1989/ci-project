# 新申请云主机搭建步骤

适用场景：应用镜像已经推送到阿里云容器镜像服务；原云主机已销毁或需要迁移到新的 Ubuntu 云主机。本手册用于恢复运行环境，不重新构建业务代码。

## 1. 先判断恢复场景

### 场景 A：旧主机上的 Jenkins、应用与数据库全部丢失

需要恢复两部分：

1. 应用运行环境：Docker、`/opt/ci-project`、镜像、数据库数据。
2. CI/CD 环境：Jenkins 容器、`jenkins_home` 备份、Registry 凭据、GitHub Webhook。

如果没有 Jenkins 备份，也可以新建 Jenkins 并按 `DEPLOYMENT_GUIDE.md` 重新配置任务和凭据。

### 场景 B：只更换应用主机，Jenkins 仍在旧主机

当前 `Jenkinsfile` 假定 Jenkins 和应用部署在同一台主机，并直接操作本机 `/opt/ci-project`。因此不能只更换应用主机后保持原流水线不变；需要二选一：

- 将 Jenkins 也迁移到新主机；或
- 后续将流水线改为通过 SSH/Agent 部署到远程应用主机。

本手册按“新主机同时运行应用，必要时也运行 Jenkins”的方式说明。

## 2. 恢复前必须准备的材料

准备以下内容，缺失时不能完整恢复：

| 项目 | 用途 |
| --- | --- |
| 已推送的前端、后端镜像 Tag | 例如同一个 Git 短 SHA 的 `frontend-<SHA>` 和 `backend-<SHA>`。 |
| `docker-compose.yml`、`init.sql` | 从 Git 仓库 `deploy/` 目录获取。 |
| 生产 `.env` 备份 | 包含数据库密码、JWT 密钥、镜像仓库地址等。不可从 Git 恢复。 |
| PostgreSQL 数据库备份 | 没有备份时只能得到全新初始化数据库，旧业务数据无法恢复。 |
| Jenkins 备份（如适用） | `/opt/jenkins/jenkins_home`、Jenkins Dockerfile、compose 文件和凭据恢复方案。 |
| 阿里云 Registry 登录凭据 | 用于拉取私有镜像。 |

> 镜像只包含应用和运行依赖，不包含 PostgreSQL 数据、`.env` 机密或 Jenkins 配置。

## 3. 新云主机基础设置

创建 Ubuntu 云主机后：

1. 放行入站 `22/tcp`（仅管理 IP）和 `80/tcp`。
2. 后续启用 HTTPS 时放行 `443/tcp`。
3. 不要开放 `5432/tcp`；PostgreSQL 只在 Docker 内部网络使用。
4. 若 Jenkins 管理页面使用 `8080`，仅允许管理 IP/VPN 访问；不要公开暴露给所有互联网来源。

安装 Docker Engine 与 Compose v2。完成后必须验证：

```bash
docker version
docker compose version
```

Ubuntu 安装 Docker Engine 请使用 Docker 官方文档对应的安装方式；不要使用 Podman 的 `docker` 兼容层运行本项目。

## 4. 准备应用部署目录

```bash
sudo mkdir -p /opt/ci-project
sudo chown -R $USER:$USER /opt/ci-project
cd /opt/ci-project
```

从 GitHub 仓库的 `deploy/` 目录复制以下两个文件到该目录：

```text
docker-compose.yml
init.sql
```

可使用 `scp` 从可信管理机上传，或在新主机克隆仓库后复制。不要将生产 `.env` 提交到 Git。

确认目录：

```bash
ls -la /opt/ci-project
```

必须看到：

```text
docker-compose.yml
init.sql
.env
```

## 5. 创建或恢复生产 `.env`

在新主机创建 `/opt/ci-project/.env`。若需要恢复旧数据库，必须恢复原来的数据库密码；数据卷中的 PostgreSQL 角色密码不会因新 `.env` 自动改变。应同时保留原 JWT 密钥，否则现有登录令牌会立即失效（通常可接受，但必须明确该影响）。只有创建全新数据库时，才生成新的强机密。

```env
IMAGE_REPOSITORY=crpi-fzzu10dkr9pvcmtt.cn-hangzhou.personal.cr.aliyuncs.com/mydev1/dev
IMAGE_TAG=<已存在的Git短SHA>

POSTGRES_DB=ci-project
POSTGRES_USER=postgres
POSTGRES_PASSWORD=<数据库原始密码>
POSTGRES_PASSWORD_URLENCODED=<同一密码的URL编码值>
JWT_SECRET=<原来的JWT密钥或新的随机密钥>
SERVER_PORT=8080
CORS_ORIGINS=*
```

`POSTGRES_PASSWORD_URLENCODED` 必须与原始密码对应。例如原始密码包含 `@` 时，编码值中应使用 `%40`。

设置权限：

```bash
sudo chown $USER:$USER /opt/ci-project/.env
chmod 600 /opt/ci-project/.env
```

`IMAGE_TAG` 必须填入镜像仓库中确实存在的短 SHA；前端和后端使用相同 SHA。

## 6. 登录阿里云镜像仓库并启动

```bash
docker login crpi-fzzu10dkr9pvcmtt.cn-hangzhou.personal.cr.aliyuncs.com

cd /opt/ci-project
docker compose pull
```

启动方式取决于是否恢复数据库：

- **无历史数据需要恢复**：直接启动完整应用。

  ```bash
  docker compose up -d
  ```

- **需要恢复历史数据库**：此时只启动 PostgreSQL，先不要启动前端和后端，更不要先开放应用给用户。

  ```bash
  docker compose up -d postgres
  docker compose ps
  ```

首次启动会拉取 `frontend-<SHA>`、`backend-<SHA>` 与 `postgres:17-alpine`，并创建 `postgres-data` Docker volume。
## 7. 验证新主机部署

检查容器状态：

```bash
cd /opt/ci-project
docker compose ps
```

三个服务都应为运行状态，前端、后端与数据库最终应显示 `healthy`。

在服务器内部验证：

```bash
docker compose exec -T frontend wget -q -O /dev/null http://127.0.0.1/
docker compose exec -T frontend wget -q -O - http://127.0.0.1/api/health
```

浏览器访问：

```text
http://<新服务器公网IP>/
http://<新服务器公网IP>/api/health
```

若内部检查成功但公网打不开，优先检查云厂商防火墙、安全组和本机防火墙是否放行 `80/tcp`。

## 8. 恢复 PostgreSQL 数据

### 8.1 有备份

逻辑备份仅恢复业务数据库，不包含 PostgreSQL 的全局角色和集群级配置；恢复时应使用与备份兼容的 PostgreSQL 客户端版本，并保留相同的数据库名和用户配置。

建议旧服务器定期生成带清理语句的业务库 SQL 备份：

```bash
cd /opt/ci-project
docker compose exec -T postgres pg_dump -U postgres --clean --if-exists --no-owner ci-project > ci-project-backup.sql
```

将备份文件保存在服务器之外。新主机仅启动 PostgreSQL 后再恢复：

```bash
cd /opt/ci-project
docker compose exec -T postgres psql -v ON_ERROR_STOP=1 -U postgres -d ci-project < ci-project-backup.sql
```

`ON_ERROR_STOP=1` 会在首个 SQL 错误时退出，避免部分恢复被误认为成功。恢复前应保留新数据卷快照或副本；恢复后检查关键表、记录数量、序列和登录功能，确认无误再启动应用：

```bash
docker compose up -d frontend backend
docker compose ps
```

生产环境应在隔离环境演练备份恢复流程。

### 8.2 没有备份

无法恢复旧业务数据。启动完整 Compose 后，新建的 `postgres-data` volume 会执行 `init.sql`，仅得到初始化表和演示数据。

不要删除 `postgres-data` volume；删除会永久丢失数据库数据。
## 9. 恢复 Jenkins（仅场景 A）

若 Jenkins 也随旧主机丢失：

1. 在新主机创建 Jenkins 自定义镜像，确保包含 Git、Docker CLI、Docker Compose 插件和所需 Jenkins 插件。
2. 挂载 `/var/run/docker.sock`、`/opt/ci-project` 和持久化的 `/var/jenkins_home`；恢复 `jenkins_home` 后确保目录 UID/GID 与 Jenkins 容器用户匹配，并按 `DEPLOYMENT_GUIDE.md` 设置 `DOCKER_GID`。
3. 若有 `jenkins_home` 备份，恢复它；否则重新创建管理员、Pipeline 任务和 `registry-credentials` 凭据。
4. 将 Pipeline 指向 GitHub 仓库 `main` 分支，脚本路径为 `Jenkinsfile`。
5. 配置 Jenkins URL、GitHub Webhook、HTTPS 和访问控制。

Jenkins 启动后验证：

```bash
docker exec jenkins git --version
docker exec jenkins docker version
docker exec jenkins docker compose version
docker exec jenkins getent ahostsv4 github.com
```

详细 Jenkins 容器和 Pipeline 配置见 `DEPLOYMENT_GUIDE.md`。

## 10. 恢复后的检查清单

- [ ] Docker 与 Docker Compose v2 可用，未使用 Podman 兼容层。
- [ ] `docker-compose.yml`、`init.sql`、`.env` 位于 `/opt/ci-project`。
- [ ] `.env` 使用正确的镜像仓库、存在的 SHA Tag、数据库变量和 JWT 密钥。
- [ ] 已成功 `docker login` 私有阿里云 Registry。
- [ ] 前端、后端、PostgreSQL 容器均运行并健康。
- [ ] 首页和 `/api/health` 均可访问。
- [ ] 云厂商安全组已放行 80/443，未放行 5432。
- [ ] 数据库备份已恢复，或已确认无历史数据需要恢复。
- [ ] 如需自动发布，Jenkins 凭据、Webhook 与 Pipeline 已恢复。

## 11. 长期改进建议

- 将 Jenkins 与应用服务器分离；Jenkins 使用受限 SSH 或专用 Agent 部署到应用主机。
- 自动执行 PostgreSQL 备份并保存到独立对象存储，定期演练恢复。
- 将 HTTPS、域名、监控告警、集中日志和镜像漏洞扫描纳入标准交付。
- 使用 `dev/test/prod` 环境和 Tag/审批发布，避免每次 `main` 提交直接更新生产环境。
