import {createRouter,createWebHistory} from 'vue-router';import Login from '@/views/LoginView.vue';import Cases from '@/views/CaseManageView.vue';import NotFound from '@/views/NotFoundView.vue';const r=createRouter({history:createWebHistory(),routes:[{path:'/login',component:Login,meta:{public:true}},{path:'/',component:Cases},{path:'/:pathMatch(.*)*',component:NotFound}]});r.beforeEach(to=>{if(!to.meta.public&&!localStorage.token)return'/login';if(to.path==='/login'&&localStorage.token)return'/'});export default r
